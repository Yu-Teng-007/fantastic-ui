/**
 * 表单组件默认配置
 */
export default {
    // 表单标签宽度
    labelWidth: "80px",

    // 表单标签位置，可选值为 left、top
    labelPosition: "left",

    // 是否禁用所有表单项
    disabled: false,

    // 是否显示星号
    showRequiredMark: true,

    // 是否显示校验错误信息
    showErrorMessage: true,

    // 是否在blur时触发验证
    validateOnBlur: true,

    // 是否在输入时触发验证
    validateOnChange: false,

    // 表单项标签对齐方式
    labelAlign: "left",

    // 验证失败时是否自动滚动到错误表单项
    scrollToError: true,
};

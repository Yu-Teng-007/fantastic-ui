/**
 * Popover 气泡弹出框默认配置
 */
export default {
    // 默认主题
    theme: "dark",
    // 默认位置
    placement: "top",
    // 默认触发方式
    trigger: "click",
    // 自动关闭延时，0表示不自动关闭
    duration: 0,
    // 默认zIndex层级
    zIndex: 100,
    // 动画持续时间
    animationDuration: 150,
};
